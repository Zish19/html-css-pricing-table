Pricing UI & Sign-Up Flow

A simple, responsive web project demonstrating a pricing plan selection interface and a connected user registration flow using pure HTML and CSS.

📂 Project Structure

price.html: The landing page displaying three pricing tiers (Basic, Standard, Premium) in a responsive card layout.

signup.html: The registration form for capturing user details (Name, Email, Mobile).

✨ Features

Responsive Design: Uses CSS Flexbox (display: flex) to ensure layout adapts to different screen sizes.

Clean UI: Minimalist aesthetic with hover effects on buttons and clear typography.

Navigation:

Pricing cards link directly to the sign-up page.

Sign-up page includes a "Back to Pricing" navigation link.

Plan Parameter: The "Sign Up" buttons in the pricing table pass the selected plan (e.g., ?plan=basic) via URL parameters to the sign-up page.

🚀 How to Run

Clone or download the repository.

Open price.html in your web browser to start the flow.

Click "Sign Up" on any card to navigate to the registration form.

🛠️ Technologies Used

HTML5 (Semantic structure)

CSS3 (Flexbox, embedded styles)
